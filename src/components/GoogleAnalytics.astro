---
// Google Analytics — GA4 via gtag.js. Rendered in <head> by Layout.astro.
// The dataLayer stub and gtag() function are set up immediately so any
// events fired before load are queued. The 153 KiB gtag.js script itself
// is injected dynamically after window load so it cannot block LCP.
// GA4 replays queued dataLayer entries when it initialises.
//
// Click tracking (mailto:, tel:, outbound) is set up here too and works
// immediately — gtag() calls before load just push to the queue.
---
<script is:inline>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-SH7NCTZDJE');

  // Inject gtag.js only after the page has fully loaded so it cannot
  // block the LCP paint. Events queued in dataLayer are replayed.
  function _loadGtag() {
    var s = document.createElement('script');
    s.src = 'https://www.googletagmanager.com/gtag/js?id=G-SH7NCTZDJE';
    s.async = true;
    document.head.appendChild(s);
  }
  if (document.readyState === 'complete') {
    _loadGtag();
  } else {
    window.addEventListener('load', _loadGtag);
  }

  // Delegated click tracking for mailto:, tel:, and outbound links.
  (function () {
    var DEST_MAP = {
      'open.spotify.com': 'spotify',
      'music.apple.com': 'apple_music',
      'www.youtube.com': 'youtube',
      'youtube.com': 'youtube',
      'youtu.be': 'youtube',
      '577records.bandcamp.com': 'bandcamp',
      'bandcamp.com': 'bandcamp',
      'www.577records.com': '577_records',
      'www.instagram.com': 'instagram',
      'instagram.com': 'instagram',
      'linktr.ee': 'linktree',
      'drive.google.com': 'gdrive'
    };

    document.addEventListener('click', function (e) {
      var a = e.target && e.target.closest ? e.target.closest('a[href]') : null;
      if (!a) return;
      var href = a.getAttribute('href');
      if (!href) return;

      if (href.indexOf('mailto:') === 0) {
        gtag('event', 'email_click', { link_url: href });
        return;
      }
      if (href.indexOf('tel:') === 0) {
        gtag('event', 'phone_click', { link_url: href });
        return;
      }

      try {
        var url = new URL(href, window.location.href);
        if (url.protocol !== 'http:' && url.protocol !== 'https:') return;
        if (!url.hostname || url.hostname === window.location.hostname) return;
        gtag('event', 'outbound_link_click', {
          destination: DEST_MAP[url.hostname] || url.hostname,
          link_url: url.href,
          link_domain: url.hostname
        });
      } catch (_) { /* malformed URL — ignore */ }
    });
  })();
</script>
